# Force LF line endings for files that run on Linux servers (VPS deploy).
# Prevents "bad interpreter: /usr/bin/env bash^M" type errors when the repo
# is edited on Windows and run on a VPS.
*.sh        text eol=lf
*.py        text eol=lf
*.service   text eol=lf
.env.example text eol=lf
