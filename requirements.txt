# Polymarket BTC 5-min UP/DOWN Trading Bot -- Python dependencies
# Install:  pip install -r requirements.txt
#
# NOTE: the dry-run / simulation engine runs on the Python standard library
# alone. Everything below is either required only for LIVE trading or is an
# optional quality-of-life upgrade. `python main.py --test-mode` works even
# with NONE of these installed.

# ── Required for LIVE trading (real orders) ───────────────────────
py_clob_client>=0.18.0      # signs & submits orders to the CLOB

# ── Optional quality-of-life (auto-detected if present) ───────────
python-dotenv>=1.0.0        # nicer .env parsing (falls back to builtin loader)
loguru>=0.7.0               # nicer logs (falls back to stdlib logging)
redis>=5.0.0               # live mode switching without restart
web3>=6.0.0                # on-chain settlement verification via RPC_URL
requests>=2.32.0           # not required (stdlib urllib is used), handy for tooling
